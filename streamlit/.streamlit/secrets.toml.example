# Copy this to secrets.toml for local dev, or paste into Streamlit Cloud Secrets UI.
# NEVER commit secrets.toml to git.

[snowflake]
account = "your-account.region"
user = "READER_SVC"
password = "your-reader-password"
database = "NZ_ELECTRICITY"
warehouse = "DASHBOARD_WH"
role = "READER"
